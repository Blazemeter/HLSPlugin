settings:
  check-updates: false

modules:
  jmeter:
    path: ".jmeter"
    version: "5.1.1"
    detect-plugins: false
    properties:
      log_level: DEBUG
      user.classpath: "libs/*"
      search_paths: "libs"
      plugin_dependency_paths: "libs"
  console:
    disable: true

files:
  - HLSSampler.jmx
  - libs

execution:
- concurrency: 1
  scenario:
    script: HLSSampler.jmx
  iterations : 3

reporting:
- module: passfail
  criteria:
  - failures>0% 